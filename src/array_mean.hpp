#ifndef WORKSHOP_ARRAY_MEAN_HPP_
#define WORKSHOP_ARRAY_MEAN_HPP_

/**
 * @brief      Calculates the mean of an integer array
 *
 * @param[in]  array     A reference to the array
 * @param[in]  size     The size of the array
 *
 * @return     The mean of the array
 */
float arrayMean(const int *array, const int size) ;

#endif